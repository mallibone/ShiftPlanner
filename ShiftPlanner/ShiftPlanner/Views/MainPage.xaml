<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ShiftPlanner"
             x:Class="ShiftPlanner.Views.MainPage"
             Title="{Binding DisplayedMonth}">

  <ListView x:Name="MonthlyShifts" ItemsSource="{Binding Days}" Margin="16,0">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <StackLayout Orientation="Horizontal" Grid.Column="0">
            <Label Text="{Binding WeekDay}" FontSize="14" FontAttributes="Bold" VerticalOptions="Center"/>
            <!--<Picker Title="Select Shift" SelectedIndex="{Binding SelectedShift}"></Picker>-->
          </StackLayout>
          <StackLayout Orientation="Horizontal" Grid.Column="1" HorizontalOptions="EndAndExpand">
            <Button Text="X" Margin="-5,0" Command="{Binding FreeShiftCommand}" WidthRequest="60" BorderColor="Blue" BorderWidth="1" BackgroundColor="White" TextColor="Blue">
              <Button.Triggers>
                <DataTrigger TargetType="Button" Binding="{Binding FreeShiftSelected}" Value="true">
                  <Setter Property="BackgroundColor" Value="Blue"/>
                  <Setter Property="TextColor" Value="White"/>
                </DataTrigger>
              </Button.Triggers>
            </Button>
            <Button Text="F" Margin="-5,0" Command="{Binding EarlyShiftCommand}" WidthRequest="60" BorderColor="Blue" BorderWidth="1" BackgroundColor="White" TextColor="Blue">
              <Button.Triggers>
                <DataTrigger TargetType="Button" Binding="{Binding EarlyShiftSelected}" Value="true">
                  <Setter Property="BackgroundColor" Value="Blue"/>
                  <Setter Property="TextColor" Value="White"/>
                </DataTrigger>
              </Button.Triggers>
            </Button>
            <Button Text="S" Margin="-5,0" Command="{Binding LateShiftCommand}" WidthRequest="60" BorderColor="Blue" BorderWidth="1" BackgroundColor="White" TextColor="Blue">
              <Button.Triggers>
                <DataTrigger TargetType="Button" Binding="{Binding LateShiftSelected}" Value="true">
                  <Setter Property="BackgroundColor" Value="Blue"/>
                  <Setter Property="TextColor" Value="White"/>
                </DataTrigger>
              </Button.Triggers>
            </Button>
            <Button Text="N" Margin="-5,0" Command="{Binding NightShiftCommand}" WidthRequest="60" BorderColor="Blue" BorderWidth="1" BackgroundColor="White" TextColor="Blue">
              <Button.Triggers>
                <DataTrigger TargetType="Button" Binding="{Binding NightShiftSelected}" Value="true">
                  <Setter Property="BackgroundColor" Value="Blue"/>
                  <Setter Property="TextColor" Value="White"/>
                </DataTrigger>
              </Button.Triggers>
            </Button>
          </StackLayout>
        </Grid>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>